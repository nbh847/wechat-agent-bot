# WeChat Agent Bot Workspace 规则

## 必读顺序

1. `README.md`
2. 本文件
3. `ROADMAP.md`
4. 与当前问题直接相关的 `docs/research/` 文档

## 项目定位

- 本仓库是 `wechat-acp` 启动 ACP Agent 时使用的默认工作目录。
- 微信通信、会话恢复和 Agent 进程由外部 `wechat-acp` 提供，本仓库不再实现自研 Bot 服务。
- 当前工作区的历史实现已归档到分支 `archive/goal7-agent-v2-20260815`，不得在 `main` 上恢复旧架构，除非散帅明确要求。
- 默认中文沟通，每次回复先称呼用户为「散帅」。代码、命令、变量名、文件名和 URL 使用英文形式。

## 约束先行

- 新功能、新目录或新数据类型出现前，先更新本文件或 `README.md` 中的结构和清理约定，再实施。
- 只修改当前任务直接涉及的文件，不顺带重构、格式化或扩展范围。
- `ROADMAP.md` 是项目状态的真实来源。只有已经完成并验证的事项才能写入“已完成”。
- 提问、讨论、调研和方案评估只做只读工作；只有散帅明确要求实施时才修改文件。

## 工作区边界

- 当前仓库是默认控制项目。自然语言提到其他项目时，只获得与本轮请求相符的只读范围，不改变默认控制项目。
- 其他项目的创建、修改、移动或删除必须取得绑定本轮任务、目标路径和具体动作的一次性确认。
- 不读取或输出 `.env`、凭据、密钥、token、私钥、登录状态和完整敏感日志，除非散帅明确授权对应内容和路径。
- 不得调用 `memex-wiki` skill，也不得通过其他 Agent 间接操作 Memex。
- 本机（Win10）龙虾目录 `D:\qclaw` 为保留资产：ROADMAP 等文档中「`~/.qclaw/` 已清理/不存在」的记录发生在 macOS 那台电脑上，不代表本机龙虾已退役。散帅明确发起清理之前，不得对 `D:\qclaw` 做任何清理、删除、迁移或写入。

## 敏感操作确认

以下操作必须在执行前通过当前对话向散帅说明具体动作、目标、潜在风险、影响范围和恢复方式，并等待明确的一次性确认：

- 删除、移动、覆盖文件或目录，以及 Git 回滚、清理、改历史和强制操作。
- 修改 `.env`、凭据、密钥、token、登录状态或其他敏感配置。
- 写入当前仓库以外的项目或目录。
- 数据库 schema 变更、数据迁移或批量修改持久数据。
- 安装全局依赖、修改系统配置或启用系统常驻服务。
- Git push、tag、release、公开发布或生产部署。
- 任何 Cloudflare 操作；每一步必须单独确认。

确认只对当次说明的目标和动作有效，不得扩展到其他路径、后续任务或同类操作。

## ACP 权限缺口

- 当前 `wechat-acp` 会自动选择 ACP 权限请求中的允许选项。
- ACP 返回“已允许”不代表散帅已经确认，不能绕过本文件的对话确认规则。
- Agent 必须在生成敏感工具调用之前先停止执行并等待散帅回复；不得先调用工具再补充说明。
- 这是行为约束而非强制沙箱。独立权限代理完成前，不得声称敏感操作已经获得技术层保护。

## 工程纪律

- 从问题根因和实际需求出发，优先最小闭环方案，不预建无真实用例的抽象、配置项或兼容分支。
- 修改代码前先检查当前项目、已有依赖、skills、plugins、MCP、Agent 内置能力和官方方案，避免重复实现。
- 修改后执行与风险相称的验证；不能通过注释错误、跳过检查或降低安全边界让结果通过。
- 密钥、token、密码和真实登录数据不得进入代码、日志或 Git。
- 常规只读 Git 可直接执行；删除、回滚、rebase、改历史、push、force-push、tag 和 release 必须先确认。
- Commit 信息使用中文描述和 `init:`、`feat:`、`fix:`、`refactor:`、`docs:` 或 `chore:` 前缀，不添加 `Co-Authored-By`。
- **涉及第三方平台查询、统计或监控时，必须优先使用已存在的 skill**（如查询智谱额度用 `glm-stats`，查询 Codex 用量用 `manage-codex-usage`），不得自行解析原始数据或手动格式化输出。

## 已犯错误（禁止重犯）

记录格式：

```
- **YYYY-MM-DD「错误名」**：发生了什么。以后如何避免。
```

- **2026-08-15「混淆本地 memory 与 Memex 入库」**：用户说「记一下入库到知识库」，我直接写到了本地 `.claude/projects/.../memory/`，没有按 Memex SOP 走完整流程。
  - **原因**：混淆了「会话临时笔记」和「Memex 正式入库」两个概念。
  - **以后**：用户说「入库到知识库」「Memex」时，必须先读 `Memex/docs/operations/ingestion-sop.md`，按候选创建 → 审核 → 批准 → 后处理的完整流程执行。本地 memory 只用于临时会话记录，不替代 Memex。
- **2026-08-17「遗漏 web-crawler 的 CDP 回退」**：官方文档直连失败且内置浏览器不可用后，我直接判断浏览器回退不可用，没有继续检查 `web-crawler` 已提供的 Edge CDP 能力。
  - **原因**：混淆了内置浏览器与独立 Edge CDP，把一个浏览器入口不可用错误扩大为所有浏览器入口不可用。
  - **以后**：使用 `web-crawler` 时必须完整执行其路由和回退链路；直连或内置浏览器失败后，继续检查 CDP 依赖与 `127.0.0.1:9222`，按 skill 脚本启动或复用 Edge CDP，确认 CDP 也失败后才能报告浏览器不可用。
- **2026-08-17「用 Git Bash ps 误判 Windows 进程状态」**：Win10 部署时后台 `wechat-acp` 任务实际存活，我用 `ps aux | grep` 得出「进程不在了」的结论并把启动命令交给用户，导致旧进程孤儿化后与新进程双消费同一微信账号，产生重复回复。
  - **原因**：Git Bash 的 `ps` 不显示 Windows 进程完整命令行，也看不到非 MSYS 派生的进程；grep 匹配不到不代表进程不存在。
  - **以后**：Windows 上确认进程存活必须用 `tasklist` 或 `wmic process where "name='node.exe'" get ProcessId,CommandLine` 这类能看完整命令行的工具；杀进程前先用它精确锁定 PID 归属，再整树清理。
- **2026-08-17「无证据归因参数失效」**：微信端收到思考转发，我在未确认该消息由哪个进程处理的情况下，断定 `--hide-thoughts` 是 bug 并准备改源码；实际是未带参数的旧进程抢走了消息，新进程参数一直生效。
  - **原因**：把旧进程的现象当成新进程的结果，跳过「先确认现象归属」直接下结论。
  - **以后**：多实例并存的排查场景，先用日志时间戳和 PID 把每个现象归因到具体进程，再定位参数或代码问题。

## 目录约定

- `README.md`：稳定定位、边界和使用入口。
- `AGENTS.md`：Codex 和通用 Agent 规则源。
- `CLAUDE.md`：Claude Code 入口，只负责引导到通用规则。
- `ROADMAP.md`：当前阶段、已完成、待办、阻塞和最近验证。
- `docs/research/`：带来源和核验日期的调研资料。
- 新目录必须先在 `README.md` 中说明用途、命名和清理规则。

## Agent 功能开发规范

- 所有通过微信 bot 操控的 Agent 开发功能，必须在 `wechat-agent-bot` 项目内完成
- 必须放在被 `.gitignore` 忽略的路径下
- 统一按轻量方式在 `personal/` 下建子目录
- 禁止在项目外开发，也禁止在未忽略路径下开发
- 示例：家庭资产盘点系统应放在 `personal/asset-inventory/`

## 交付自检

- 每行改动都能追溯到散帅的当前请求。
- 没有读取、修改或删除未授权的敏感数据。
- 没有用 ACP 自动批准代替人工确认。
- 已执行适当验证并如实更新 `ROADMAP.md`。
