# WeChat Agent Bot Codex 规则

## 必读顺序

1. `README.md`
2. 本文件
3. `ROADMAP.md`
4. 与当前问题相关的 `docs/research/` 文档

## 当前阶段

Goal 1 至 Goal 6 均已由散帅完成人工验收，MVP 已完成。当前进行 Goal 7：优化自然对话路由与本机只读授权；不得未经对应红线授权安装或启用系统常驻配置，不得把核心工作流绑定到 Codex、Claude Code 或其他具体 Agent。

## 项目边界

- 项目入口是微信，执行端是本地 AI Agent，但不得绑定某一种 Agent。
- 本项目是 AI-native 项目：任何 Agent 进入仓库后，必须先读根目录 `README.md`、本文件和 `ROADMAP.md`，再按当前 Goal 及其人工验收 Gate 工作。工具专用规则文件只能引导到这套通用工作方式，不得另建相互冲突的流程。
- 微信下发的任务可以只读访问配置的工作区下其他项目；未经散帅针对当前任务和具体目标项目的二次确认授权，不得在其他项目中创建、修改、移动或删除任何文件，也不得执行其他会改变项目状态的操作。
- Agent 的控制项目始终是 `wechat-agent-bot`。自然语言明确提到其他项目时，该项目只作为本轮任务目标，不得因此改变控制项目或后续新需求的默认目标；只有显式“切换项目”才设置持久默认目标。
- 散帅在任务中明确点名 `~/...` 本机路径，或明确要求检查本机工具配置时，该请求本身即构成本轮只读授权；已授权读取路径可在同一 Agent 会话续接，新会话、跨自然日或达到轮次上限后清空。不得把只读授权推断为写入授权。
- Bot 不从自然语言猜测项目、应用、配置、任务或业务动作；这些判断由当前 Agent 完成。Bot 只把用户原话和有限发现范围交给 Agent，再对 Agent 返回的结构化计划重新校验路径、模式和确认边界。
- Agent 发现本机工具目录时，只能使用当前请求的有限只读范围，跳过备份、缓存、日志、媒体和诊断目录；不得把目录发现扩大为全盘读取。
- 本机路径只读不得读取或输出 `.env`、登录凭据、密钥、token 等敏感内容，除非散帅另行明确授权对应敏感操作；配置工作区之外的写入不属于普通任务能力。
- 跨项目写入授权不得从“允许读取”“允许执行当前项目任务”或此前其他任务的授权中推断；每次授权必须绑定任务 ID、目标项目和拟执行动作，授权一次后失效。
- 微信接入、安全任务入口、Agent-neutral 执行、连续会话和本地常驻运行均已完成验证；Goal 7 新增能力只有完成自动化与真实微信验收后才能写成已完成。
- 涉及非官方微信协议、账号风控、隐私、远程命令执行和本机权限时，必须明确证据来源、风险与残余不确定性。
- 不得读取或写入真实微信数据、登录凭据、密钥、token、`.env` 或其他项目数据，除非散帅另行明确授权。
- 正式 iLink 通道的登录凭据、同步状态和二维码只能写入 `runtime-data/ilink/`；早期验证数据保留在 `runtime-data/ilink-smoke/`。两者均不得输出内容、提交或复制到其他目录，是否清理由散帅决定。
- 微信绑定凭证固定保存在项目内的 `runtime-data/ilink/state.json`。只要服务端未明确判定凭证失效，启动和重连都必须复用现有凭证，不得主动、定期或因普通网络错误刷新凭证；只有服务端明确返回 token 失效时，才清除失效凭证并进入重新扫码流程。
- 微信指令产生并需要长期保留的业务数据统一写入 `runtime-data/user-data/`，例如待办事项、提醒和定时任务定义；按功能建立小写英文子目录，不得写入 `ROADMAP.md`、源码目录或其他项目。该目录只保存数据，不保存可执行代码、凭据、密钥、token 或日志，整体禁止提交，是否清理由散帅决定。
- 当前上下文内的微信用户文本和 Bot 回复统一写入 `runtime-data/conversations/state.json`；消息只保存 `role`、`text` 和时间，另保存上下文 ID 与发送者 ID 作为归属信息，不保存微信原始报文、凭据、token 或模型内部推理。对话默认保留 30 天、最多 30 个上下文、每个上下文最多 200 条消息；显式新会话、跨自然日或达到 30 次 Agent 调用时切换上下文。
- 持久化状态必须同时具备时间和数量边界：已结束任务默认保留 90 天且最多 1000 条，微信上下文 token 默认保留 30 天且最多 100 个，待发送回复默认保留 7 天且最多 1000 条，消息去重 ID 最多 1000 个；运行中、排队和待确认任务不得因过期策略删除。
- 登录凭证只在服务端明确判定失效时清理，不采用固定 TTL；待办、提醒和定时任务等业务数据按完成、取消或失效状态制定各自清理规则，不得仅按创建时间盲目删除。
- 不得调用 `memex-wiki` skill 或通过其他 Agent 间接操作 Memex。

## 文档规则

- `README.md` 只记录稳定定位、边界和入口。
- `ROADMAP.md` 是项目进度的真实来源。
- 调研材料放在 `docs/research/`，区分事实、推断、待验证问题和建议。
- 调研结论必须标注来源与核验日期；优先使用官方文档、官方 SDK 或可运行参考实现。
- 架构决策只有经散帅确认后才能写入 `docs/decisions/`。
- 新目录创建前，先在 `README.md` 补充用途、命名和清理约定。

## 变更与验证

- 只修改当前任务直接涉及的文件，不顺带重构或扩展范围。
- 开发严格遵循 `docs/plans/development-goals.md`：一次只执行一个 Goal；当前 Goal 完成并验证后停止，只有散帅明确验收通过才开始下一个 Goal。
- 文档或调研导致项目状态变化时，同步更新 `ROADMAP.md`；只有稳定定位、阶段或入口变化时才更新工作区 `WORKSPACE.md`。
- 文档变更至少检查文件存在、Markdown 结构、相互引用和 `git diff --check`（如项目已纳入 Git）。
- 删除文件或目录、修改凭据或 `.env`、安装全局依赖、公开部署及其他全局规则红线操作，必须先取得散帅确认。
