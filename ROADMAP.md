# WeChat Agent Bot Roadmap

## 当前阶段

Goal 1 至 Goal 6 均已由散帅完成人工验收，MVP 已完成。Goal 7 正在把自然语言解析、项目／应用发现和业务判断下沉到 Agent；Bot 只保留通讯、上下文、生命周期和安全边界。

## 已完成

- 确定项目名为 WeChat Agent Bot，目录名为 `wechat-agent-bot`。
- 明确项目不绑定 Codex、Claude Code 或其他具体 AI Agent。
- 建立项目定位、Agent 规则、阶段进度和调研入口文档。
- 完成微信双向消息接入工具初步调研，确认 iLink Bot API 是首选验证路线；结论见 `docs/research/wechat-access-options.md`。
- 确定服务采用 TypeScript + Node.js，腾讯官方 `openclaw-weixin` 作为协议基准，`@wechatbot/wechatbot` 作为首选独立 SDK 候选。
- 核对本机环境：macOS 13.7.8 x86_64、Node.js 24.15.0 和 npm 11.12.1 满足 SDK 的 Node.js 22+ 要求。
- 完成 iLink 本机最小闭环：扫码登录，接收 `ping` 并回复 `Echo: ping`。
- 完成凭证持久化和免扫码重启验证：接收 `ping2` 并回复 `Echo: ping2`。
- 确认 `@wechatbot/wechatbot@2.2.0` 登录轮询存在超时处理缺陷；最小验证改用与腾讯官方源码逐字段对齐的 HTTP 客户端完成。
- 确认第一版不使用第三方微信 SDK，在项目内维护最小 iLink 通道适配器；决策见 `docs/decisions/001-ilink-channel-implementation.md`。
- 将 MVP 拆为 6 个严格串行的开发 Goal；范围和验收标准见 `docs/plans/development-goals.md`。
- 明确跨项目边界：微信任务可只读其他项目；任何跨项目写入必须取得绑定任务、目标项目和具体动作的一次性二次确认。
- 完成项目内最小 iLink 文本通道：扫码登录、二维码过期刷新、凭证与游标恢复、文本收发、去重、待回复重试、token 失效处理和优雅停止。
- 完成 12 个自动化测试，并通过类型检查和依赖审计。
- 完成真实微信验收：二维码过期自动刷新、扫码登录、文本收发、Echo 回复及单实例仅回复一次均已验证。
- 固化凭证生命周期：绑定凭证保存在项目内并在有效期内持续复用，仅在服务端明确返回 token 失效时清除并重新扫码。
- 散帅完成人工验收：凭证免扫码复用、单实例文本收发、重启恢复和正常停止均通过。
- 完成安全任务入口：绑定用户校验、工作区路径约束、任务持久化、状态查询、取消和拒绝。
- 完成跨项目写入与红线操作二次确认：确认码绑定任务 ID、目标项目和具体动作，错误码无效且正确码只能使用一次。
- 完成真实微信联调：跨项目写入进入确认态，错误确认码被拒绝，正确确认生效，重复确认被拒绝，服务重启后任务授权状态恢复。
- 散帅完成人工验收：普通任务记录与取消、跨项目写入拒绝、确认码失效和路径越界均符合预期。
- 完成 Agent-neutral JSON 进程协议、执行协调器、单任务互斥、项目上下文清单、超时、取消、失败状态和结果持久化回传。
- 明确权限责任：核心服务传递最大读写路径，具体 Agent 适配器必须用沙箱／权限机制落实，不得仅依赖提示词。
- 完成真实 Agent 只读闭环：微信下发 `T0006`，临时合规适配器在 `repo-scout` 只读执行并将两句结果回传微信；目标项目未产生改动。
- 散帅完成人工验收：`T0006` 成功从真实 Agent 回传与 `repo-scout` 当前状态一致的结果，Goal 3 验收通过。
- 完成微信会话、当前任务、默认目标项目、执行适配器和 Agent 会话标识的持久化绑定。
- 完成口语化只读任务、明确项目名自动路由、显式项目切换、任务续接、新会话、当前状态和当前任务取消；同时提到多个项目或无法识别目标时才询问。
- 完成 Agent `sessionId` 续接、单运行任务加一个等待任务、排队开始通知、两分钟一次的有限状态反馈和重启中断恢复。
- 口语化写入统一进入二次确认，显式指令继续沿用 Goal 2 权限规则；执行队列已满时任务明确失败且不覆盖当前会话。
- 完成三层上下文策略：默认持续当前 Agent 会话；上海时区跨自然日或累计 30 次 Agent 调用后自动轮换；“新任务／新会话”立即轮换。自动轮换只传递最多 1000 字的状态交接摘要，不携带完整聊天记录。
- 完成真实微信 Goal 4 验收：口语化跨项目只读、固定 Agent 工作目录、跨项目连续上下文、显式新会话隔离、状态查询、取消和跨项目写入拒绝均符合预期。
- 修复人工验收发现的只读“发布前风险”误触高风险确认，以及并发 flush 导致同一任务结果重复回传的问题。
- 完成项目内单实例锁、失效锁恢复、启动／停止／状态命令、最小健康状态、日志脱敏和有限轮转。
- 交付未安装的 macOS `launchd` 配置模板与运行说明；未写入 `~/Library/LaunchAgents`，未执行加载或启用。
- 完成真实运行验证：第二实例被锁拒绝，正常停止释放锁，后台重启免扫码恢复，`T0019` 未重复执行，`T0020` 续接 Agent 会话成功。
- Goal 6：运行与故障排查说明、端到端安全验收记录、依赖／许可证／敏感数据／日志边界复核和正式 Codex 适配器均已完成并通过人工验收；`launchd` 仍未安装或启用。

## 待调研

- iLink 的手机端交互形态、群聊能力、主动消息边界、解绑恢复和限流规则。
- 本地常驻服务的运行方式及消息可靠性要求。
- 多种 AI Agent 的统一调用边界和能力差异。
- 发信人身份识别、授权确认和高风险操作拦截。
- 长任务的状态反馈、中断、超时、重试与结果回传。
- 会话上下文、任务记录、敏感数据和审计日志的保存边界。
- Goal 4 口语化任务解析的意图结构和安全回显方式；会话路由采用显式指令优先、默认续接当前任务、歧义时询问的固定规则，不在历史任务间做语义猜测。

## Goal 顺序

1. Goal 1：正式化最小 iLink 文本通道。
2. Goal 2：安全任务入口与确认协议。
3. Goal 3：单 Agent 执行闭环。
4. Goal 4：会话连续性与任务生命周期。
5. Goal 5：本地常驻运行与故障恢复。
6. Goal 6：MVP 端到端安全验收。

当前 Gate：Goal 7 进行中；完成自动化与真实微信验证后等待散帅人工验收。

## 阻塞

- Goal 5 实际安装 `launchd` 配置前，需要散帅单独批准修改系统常驻配置。

## 最近验证

- 2026-08-13：检查项目必需文档、入口引用和工作区索引登记。
- 2026-08-13：核对腾讯 iLink 官方仓库、官方源码及主要第三方 SDK，完成微信接入初步调研文档。
- 2026-08-13：在真实微信会话完成两轮文本收发，并验证凭证持久化和免扫码重启恢复；`npm run check` 通过，依赖审计为 0 个漏洞。
- 2026-08-13：记录项目内最小 iLink 适配器决策，并建立 6 个串行开发 Goal 及逐 Goal 人工验收 Gate。
- 2026-08-13：Goal 1 类型检查通过，12 个自动化测试通过，`npm audit` 报告 0 个漏洞；正式状态文件和登录二维码权限均验证为 `0600`。
- 2026-08-13：真实微信验证二维码自动刷新、登录、文本接收和回复；单实例运行时 `goal1-single-instance-test` 仅收到一条 Echo。双实例会各自回复，单实例锁留待 Goal 5。
- 2026-08-13：确认正式绑定凭证固定保存在 `runtime-data/ilink/state.json`；现有恢复逻辑在凭证未失效时直接复用，不触发扫码或凭证刷新。
- 2026-08-13：散帅手工验证 Goal 1 通过；Goal 1 标记为已确认，开始 Goal 2。
- 2026-08-13：Goal 2 类型检查通过，22 个自动化测试通过，`npm audit` 报告 0 个漏洞；任务状态文件权限验证为 `0600`。
- 2026-08-13：真实微信验证跨项目写入确认、错误码拒绝、正确码一次性使用、状态查询及重启恢复；只读 Git 状态确认 `repo-scout` 未被修改。
- 2026-08-13：散帅手工验证 Goal 2 通过；Goal 2 标记为已确认。
- 2026-08-13：明确项目采用 AI-native、Agent-neutral 工作方式；所有 Agent 读取同一套仓库规则与 Roadmap，Goal 3 不预先绑定具体 Agent。
- 2026-08-13：确认口语化任务解析纳入 Goal 4；默认自然语言入口与显式命令并存，解析不得绕过 Goal 2 的确认边界。
- 2026-08-13：确认 Goal 4 持久化“微信会话—当前任务—执行适配器—Agent 会话—项目”绑定；连续消息默认续接当前任务，语义不得用于猜选历史 `taskId`。
- 2026-08-13：Goal 3 类型检查通过，31 个自动化测试通过，`npm audit` 报告 0 个漏洞；任务、iLink 状态和二维码文件权限均验证为 `0600`。
- 2026-08-13：真实微信验证 `T0005` 执行端启动失败回传和 `T0006` 真实 Agent 只读成功回传；只读 Git 状态确认 `repo-scout` 未被修改。
- 2026-08-13：散帅确认 Goal 3 人工验收通过；项目 Gate 切换为 Goal 4 待开始，尚未开始 Goal 4 实现。
- 2026-08-13：散帅确认 Goal 4 实施计划；Goal 4 标记为进行中，本轮验证命令为 `npm run check`、`npm test`、`npm audit` 和 `git diff --check`。
- 2026-08-13：Goal 4 类型检查通过，44 个自动化测试通过，`npm audit` 报告 0 个漏洞，`git diff --check` 通过；人工验收发现并修复只读“发布风险”误触确认和明确项目名仍要求手动切换的问题，真实微信人工验收待继续。
- 2026-08-13：人工验收进一步明确执行模型：Agent 永远从 `wechat-agent-bot` 路径运行，其他项目只是每轮动态目标和权限范围；改变目标项目不新建 Agent 会话，仅“新任务”或显式切换历史任务改变上下文。
- 2026-08-13：确认并实现三层上下文策略：默认连续、跨自然日或 30 轮自动换新、显式新任务立即换新；自动轮换使用最多 1000 字的最小交接摘要控制 token 消耗。
- 2026-08-13：真实微信验收 `T0013` 至 `T0018`：结果单次回传、跨项目连续上下文、新会话隔离、取消和写入拒绝均通过；散帅确认 Goal 4 验收通过。
- 2026-08-13：散帅确认 Goal 5 实施计划；Goal 5 标记为进行中，不安装或启用 `launchd` 系统配置。
- 2026-08-13：Goal 5 类型检查通过，49 个自动化测试通过，`npm audit` 报告 0 个漏洞，`git diff --check` 通过；单实例、控制命令、`0600` 文件权限和日志轮转验证通过。
- 2026-08-13：真实微信验证后台停止／重启免扫码恢复；`T0019` 未重复执行，`T0020` 在重启后续接上一轮 Agent 上下文并成功回传。
- 2026-08-13：散帅确认 Goal 5 人工验收通过；项目 Gate 切换为 Goal 6 待开始，`launchd` 未安装或启用。
- 2026-08-13：散帅确认开始 Goal 6；本轮只做 MVP 端到端安全复核和交付收尾，不新增核心功能，开机自启继续仅记录为可选方案。
- 2026-08-13：正式 Codex 适配器完成自动化与隔离目录真实验证：普通只读从控制项目运行并续接原 session，已确认写入临时从目标项目运行且 session 不持久化；权限组合不一致时失败关闭。
- 2026-08-13：正式 Codex 适配器真实微信验证通过：`T0021` 跨项目只读、`T0022` 连续追问、`T0023` 确认前零写入和确认后目标项目单文件写入、`T0024` 返回原只读会话均符合预期；Goal 6 等待最终人工验收。
- 2026-08-13：散帅确认 Goal 6 人工验收通过；Goal 1 至 Goal 6 全部完成，MVP 标记为完成。
- 2026-08-13：将普通任务开始提示改为“收到，正在生成回复中...”，成功结果直接显示 Agent 回复，不再暴露任务 ID 和内部成功状态；异常结果与状态控制仍保留任务 ID 便于追踪。
- 2026-08-14：散帅确认 Goal 7 实施方案：控制项目固定为 `wechat-agent-bot`，其他项目仅作为本轮目标；支持会话级本机只读路径，项目内低风险写入直接执行，跨项目写入和红线操作继续二次确认。
- 2026-08-14：Goal 7 的旧实现曾把自然语言路由和应用发现放在 Bot；复查后确认这违反 Agent-native 边界，旧实现不作为完成能力保留，转入 Agent 计划／执行协议重构。
- 2026-08-14：移除长任务开始和进度提示中的内部任务 ID；日常进度改用“已开始生成回复”“仍在生成回复中”，任务 ID 仅保留给确认、显式控制和异常排障。
- 2026-08-14：二次确认改为当前会话直接“确认／拒绝”，多待确认操作按项目或动作关键词消歧；内部任务 ID 和旧式确认命令继续兼容。
- 2026-08-14：确认 Bot 与 Agent 的责任边界：自然语言、项目／应用发现、配置／任务读取和业务判断由 Agent 负责；Bot 只校验结构化计划、处理确认、权限和生命周期。本轮开始实现两阶段 Agent 协议，真实微信回归尚未完成。
- 2026-08-14：完成 Agent 协议 v2 两阶段分析／执行、结构化计划路径校验、自然语言原话委托和 Codex 适配器隔离；`npm run check`、58 个自动化测试、`npm audit`（0 vulnerabilities）和 `git diff --check` 通过。服务已重启验证监听，真实微信回归待散帅验收。
- 2026-08-14：修复 QClaw 非敏感任务配置读取因“跳过凭据／token”等否定描述误触确认的问题；敏感访问改由 Agent 结构化声明并结合实际路径校验，不再扫描业务动作文本。`npm run check`、58 个自动化测试、`npm audit`（0 vulnerabilities）和 `git diff --check` 通过，真实微信回归待继续。
